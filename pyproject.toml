[project]
name = "ntu-certificate-monitor"
version = "0.1.0"
description = "Watch NTU course certificate availability and notify via ntfy"
requires-python = ">=3.12"
dependencies = ["beautifulsoup4>=4.13,<5"]

[tool.uv]
package = false
